name: .NET Core CI

on:
  push:
    branches: [ main ]

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

  setup-dotnet:
    runs-on: ubuntu-latest
    steps:
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.0'

  restore:
    runs-on: ubuntu-latest
    steps:
      - name: Restore NuGet packages
        run: |
          cd D:/VS Projects
          dotnet restore
    needs: [setup-dotnet]

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: |
          cd D:/VS Projects
          dotnet build -c Debug
    needs: [restore]

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run unit tests
        run: |
          cd D:/VS Projects
          dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=opencover /p:CoverageThreshold=80 MyProject.csproj
    needs: [build]

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub
        run: |
          git config --global user.name 'Musayyab Ali'
          git config --global user.email 'meharmusayyab713@gmail.com'
          git add .
          git commit -m "Automated deployment"
          git push origin main
    needs: [test]
